/**
 * Schema configuration file
 * Change global zod configuration here
 * Replace import * as zod from "zod"; with import zod from "@schemas/_schema.config"; in other schema files after modifying this file
 */
import * as zod from "zod";

zod.config();

export default zod;
