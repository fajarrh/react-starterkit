/**
 * 
 * @param {*} column
 * @param {*} modelName
 * @description Template for create page   
 * @returns {string} Template string
 */

const createTemplate = ({ column, modelName }) => {
  return `/** Template for create of ${modelName} */`;
};
