/**
 * 
 * @param {*} column
 * @param {*} modelName
 * @description Template for index page 
 * @returns {string} Template string
 */

const indexTemplate = ({ column, modelName }) => {
  return `/** Template for index of ${modelName} */`;
};
