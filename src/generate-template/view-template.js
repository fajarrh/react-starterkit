/**
 * 
 * @param {*} column
 * @param {*} modelName
 * @description Template for view page   
 * @returns {string} Template string
 */

const viewTemplate = ({ column, modelName }) => {
  return `/** Template for view of ${modelName} */`;
};
