import { styled } from "@mui/material/styles";

const InputDateCell = styled("input")(() => ({
  border: "none",
  outline: "none",
  backgroundColor: "transparent",
  width: "100%",
  fontSize: "inherit",
  fontFamily: "inherit",
  padding: 0,
}));

export default InputDateCell;
